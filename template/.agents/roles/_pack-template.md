# {Role Name} role pack

<!--
Copy this file to {role-key}.md and fill it in to add a new role.
{role-key} is the value users set in config.yaml → owner.role.
Delete these HTML comments when done. See docs/roles.md for the full guide.
-->

**Role key:** `{role-key}`  ← value for `config.yaml` → `owner.role`
**Status:** `example`  ← `shipped` (ships with emOS) · `community` · `example`

## Who this is for

{One or two lines: the job this role does and how it differs from the others.}

## Vocabulary

Tokens role-sensitive skills resolve from this pack (see [README.md](README.md) for the
full contract):

| Token | Value for this role |
|-------|---------------------|
| `{{role_report_slug}}` | `{slug}` |
| `{{role_report_title}}` | `{title, may use {{team}}}` |
| `{{review_dimensions}}` | `{Dim1, Dim2, Dim3, Dim4, Dim5}` |
| `{{oneonone_default_relation}}` | `{report \| manager \| peer \| stakeholder}` |
| `{{status_report_audience}}` | `{who reads the status report}` |

## People & relationships

{What "people" means for this role, and the default relation. Which relations matter.}

## Sections in focus / dormant

{Which numbered folders + dashboards this role uses often vs. rarely. Folders exist for
every role — this is emphasis only, not relocation.}

## Skill notes

- **1on1** — {framing for this role's typical 1-on-1}
- **perf-review** — {self vs. others; which dimensions; any role caveat}
- **manager-report** — {what the status report emphasizes and who it's for}
- **morning / weekly-review** — {any role-specific surfacing}
