# emOS Personalization — CONFIG.md

_Your personal, human-facing config. `emos update` never touches it. **Copy this
file to `CONFIG.md`** and fill it in — any agent working in this vault reads it
alongside `.agents/config.yaml` for context._

> **Two config files, different jobs.** `.agents/config.yaml` holds *structured*
> settings (people map, paths, tool selection, integration coordinates). This
> file, `CONFIG.md`, holds *free-form prose* that doesn't fit structured fields —
> who you are, what you're focused on, and how you like to work.

## Who I am

- **Name:**
- **Role:**
- **Team:**

## What I'm using emOS for

_A sentence or two on your current focus, priorities, or what you're trying
to get out of this system._

## Working style notes

_Anything an agent should know about how you like to work — communication
style, what to flag vs. handle silently, how direct to be, etc._
